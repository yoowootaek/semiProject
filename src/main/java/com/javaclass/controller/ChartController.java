package com.javaclass.controller;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.javaclass.domain.ChartVO;
import com.javaclass.service.ChartService;

@Controller
public class ChartController {

	/*
	 * @RequestMapping("admin/adminHome.do") public void adminHome() { }
	 */
	
	/*
	 * @Autowired private ChartService mapper;
	 * 
	 * 
	 * @RequestMapping("/chart.do") public @ResponseBody List<ChartVO>
	 * countBoard(Model m, ChartVO vo) throws Exception{ List<ChartVO> chart =
	 * mapper.countBoard(vo); m.addAttribute("chart", chart); return chart; }
	 */
	
	
}
